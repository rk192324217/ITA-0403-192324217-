# Vectors
vector_a <- c(1, 2, 3)
vector_b <- c(4, 5, 6)

# Matrices
matrix_3x3 <- matrix(1:9, nrow = 3)

# Function
sum_function <- function(x, y) {
  return(x + y)
}

# List
elements_list <- list(vectors = list(vector_a, vector_b), matrix = matrix_3x3, sum_func = sum_function)
elements_list
